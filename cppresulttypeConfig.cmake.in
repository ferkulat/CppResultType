@PACKAGE_INIT@

cmake_policy(SET CMP0011 NEW) # push pop policies at include



list(APPEND CMAKE_MODULE_PATH ${CMAKE_CURRENT_LIST_DIR}/cmake/modules)

message("PACKAGE_PREFIX_DIR ${PACKAGE_PREFIX_DIR}")
foreach(component ${cppresulttype_FIND_COMPONENTS})
    include(@PROJECT_BINARY_DIR@/components/${component}/cppresulttype-cppresulttype_${component}-config.cmake)
endforeach()